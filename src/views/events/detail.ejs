<!doctype html>
<html lang="en">

<head>
    <title>Detalle</title>
</head>

<body>
    <h1>
        <%= event.name %>
    </h1>
    <img src="<%= event.image %>" alt="<%= event.name %> image" style="max-width: 150px; max-height: 100px;" /></a>
    <% if (current_user && (event.organizer._id.equals(current_user._id))) { %>
        <form class="float-right" action="/eventos/<%= event._id %>?_method=DELETE" method="POST">
            <button class="btn btn-sm delete-btn px-3"><svg xmlns="http://www.w3.org/2000/svg" width="20" height="20"
                    fill="currentColor" class="bi bi-trash-fill" viewBox="0 0 16 16">
                    <path
                        d="M2.5 1a1 1 0 0 0-1 1v1a1 1 0 0 0 1 1H3v9a2 2 0 0 0 2 2h6a2 2 0 0 0 2-2V4h.5a1 1 0 0 0 1-1V2a1 1 0 0 0-1-1H10a1 1 0 0 0-1-1H7a1 1 0 0 0-1 1H2.5zm3 4a.5.5 0 0 1 .5.5v7a.5.5 0 0 1-1 0v-7a.5.5 0 0 1 .5-.5zM8 5a.5.5 0 0 1 .5.5v7a.5.5 0 0 1-1 0v-7A.5.5 0 0 1 8 5zm3 .5v7a.5.5 0 0 1-1 0v-7a.5.5 0 0 1 1 0z" />
                </svg></button>
        </form>
        <% } %>

            <h4>Fecha(s) <%= event.date %>
            </h4>
            <h4>Horario <%= event.time %>
            </h4>
            <h4>Descripción <%= event.description %>
            </h4>
            <h4>Creado el <%= event.createdAt %> por <%= event.organizer.name %>
            </h4>
            <h4>Categoría <%= event.category %>
            </h4>
            <h4>Tag <%= event.tag %>
            </h4>

</body>